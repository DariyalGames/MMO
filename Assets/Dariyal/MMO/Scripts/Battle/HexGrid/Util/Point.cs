using System;

namespace Dariyal.MMO.Battle.HexGrid
{

    //struct holding x and y coordinates
    public struct Point
    {
        public int X, Y;

        public Point(int x, int y)
        {
            X = x;
            Y = y;
        }
    }
}
